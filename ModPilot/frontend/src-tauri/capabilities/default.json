{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window: native dialog access for the Browse buttons. Drag-and-drop file events are a built-in webview event in Tauri v2 and need no explicit grant.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
